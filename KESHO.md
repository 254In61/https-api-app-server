# What I will be doing tommorow
- set up server/setup.sh ... Intention is to have a script that reads modules from some .txt files and installs them.
- Clone down again the repo on the-eagle and test.
